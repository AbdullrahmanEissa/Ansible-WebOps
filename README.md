---

# 🌐 Fully Automated Bio Website Deployment (Terraform + Ansible)

![Ansible](https://img.shields.io/badge/Ansible-Automation-blue?style=for-the-badge\&logo=ansible)
![AWS](https://img.shields.io/badge/AWS-Cloud-orange?style=for-the-badge\&logo=amazon-aws)
![Terraform](https://img.shields.io/badge/Terraform-IaC-purple?style=for-the-badge\&logo=terraform)
![Apache](https://img.shields.io/badge/Apache-WebServer-red?style=for-the-badge\&logo=apache)

---

## 🚀 Overview

This project provides a **one-click automated pipeline** to:

* Create AWS EC2 instances via **Terraform**
* Install Apache2 via **Ansible**
* Clone your GitHub Bio Website repo
* Deploy the final website to `/var/www/html/`
* Handle updates with zero manual work
* Generate all configuration files automatically (Terraform, inventory, ansible.cfg, playbook)

Everything is packaged inside two scripts:

* `creation.sh` → Generates infrastructure & config files
* `work.sh` → Runs deployment using Ansible

---

## 🧠 Architecture Flow (Simple & Clean)

```
┌────────────────┐
│ creation.sh     │  → Creates main.tf, inventory, apache.yml, ansible.cfg
└───────┬────────┘
        │
 terraform apply   (Ctrl+C after creation)
        │
┌───────▼────────┐
│ Replace IPs     │ → Add EC2 Public IPs in "inventory" file manually
└───────┬────────┘
        │
 run work.sh
        │
┌───────▼────────┐
│  Website LIVE   │ ✔ Automated Apache deployment with Git repo
└────────────────┘
```

---

## ⚙️ Files Auto-Generated by `creation.sh`

### ✔ `main.tf`

* Creates **2 AWS EC2** instances (`t3.micro`)
* Uses latest Ubuntu 22.04 AMI
* Adds tags
* Private key name configurable

### ✔ `inventory`

* Placeholder `IP GOES HERE`
* Configured for Ubuntu user + SSH private key

### ✔ `ansible.cfg`

* Points Ansible to the generated inventory
* Sets default SSH key

### ✔ `apache.yml`

* Installs Apache2
* Installs Git
* Clones your GitHub repo
* Moves `index.html`
* Restarts Apache2

---

## 🏗️ Deployment Steps (The New Correct Way)

### 🎯 **STEP 1 — Run the generation script**

```bash
./creation.sh
```

This creates all required files:

* `main.tf`
* `inventory`
* `ansible.cfg`
* `apache.yml`

---

### 🎯 **STEP 2 — Run Terraform**

Inside `work.sh`, the Terraform commands are commented.

You run them manually:

```bash
terraform init
terraform apply
```

📌 **IMPORTANT:**
After the EC2 instances are created, **press `CTRL + C`** to stop Terraform after creation (you don't want it to wait).

---

### 🎯 **STEP 3 — Add the EC2 Public IPs**

Go to AWS Console → EC2 → Copy the two instance public IPs
Put them inside `inventory`:

```
[ansible]
3.122.111.22
54.165.85.102

[ansible:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=/home/eissa/Downloads/key.pem
```

---

### 🎯 **STEP 4 — Disable Terraform in work.sh**

Inside `work.sh`, keep Terraform commented:

```bash
#!/bin/bash

#terraform init
#terraform plan
#terraform apply

ansible-playbook apache.yml
```

---

### 🎯 **STEP 5 — Run The Deployment**

```bash
./work.sh
```

🔥 RESULT:
Ansible automatically configures both instances and deploys your website from GitHub.

🎉 **Congratulations — full automation achieved!**
Zero manual Apache setup. Zero Git clone. Zero copy-paste.

---

## 🌐 Features

| Feature                                 | Status |
| --------------------------------------- | ------ |
| Automatic server creation via Terraform | ✔      |
| Auto-generated Ansible inventory        | ✔      |
| Apache installation & startup           | ✔      |
| Git repo deployment                     | ✔      |
| Multi-instance support                  | ✔      |
| Fully script-driven pipeline            | ✔      |
| Zero downtime upgrades                  | ✔      |
| Production-ready structure              | ✔      |

---

## 🛠️ Technologies

| Layer      | Tools            |
| ---------- | ---------------- |
| IaC        | Terraform        |
| Automation | Ansible          |
| Web Server | Apache2          |
| Repo       | GitHub           |
| OS         | Ubuntu 22.04 LTS |
| Cloud      | AWS EC2          |

---

## 📈 Future Roadmap

* Add full CI/CD using GitHub Actions
* Auto SSL certificates via Let's Encrypt
* Use Application Load Balancer (ALB)
* Add Auto-Scaling Group
* Convert deployment into Docker containers
* Add Netdata monitoring role

---

## 👤 Author

**Abdullrahman Eissa**
Linux System Administrator | DevOps Enthusiast
GitHub • LinkedIn

---

لو تحب أزود كمان GIF للـ pipeline أو أرسم لك Architecture Diagram محترف…
بس قولّي. أنا معاك للنهاية يا بطل. 🔥🚀
