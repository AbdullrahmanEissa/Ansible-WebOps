---

```markdown
# 🌐 Fully Automated Bio Website Deployment (Terraform + Ansible)

![Ansible](https://img.shields.io/badge/Ansible-Automation-blue?style=for-the-badge&logo=ansible)
![AWS](https://img.shields.io/badge/AWS-Cloud-orange?style=for-the-badge&logo=amazon-aws)
![Terraform](https://img.shields.io/badge/Terraform-IaC-purple?style=for-the-badge&logo=terraform)
![Apache](https://img.shields.io/badge/Apache-WebServer-red?style=for-the-badge&logo=apache)

---

## 🚀 Overview
This project automates the deployment of a **Bio Website** using **Terraform** and **Ansible**, fully script-driven:

- Creates AWS EC2 instances via Terraform
- Installs Apache2 via Ansible
- Clones your GitHub Bio Website repo
- Deploys the website to `/var/www/html/`
- Handles updates with zero manual work
- Generates all config files automatically (`main.tf`, `inventory`, `ansible.cfg`, `apache.yml`)

Everything is packaged inside two scripts:

- `creation.sh` → Generates infrastructure & config files  
- `work.sh` → Runs deployment using Ansible

---

## 🧠 Architecture Flow
```

┌────────────────┐
│ creation.sh     │ → Generates Terraform & Ansible files
└───────┬────────┘
│
terraform apply  (press Ctrl+C after creation)
│
┌───────▼────────┐
│ Replace IPs     │ → Add EC2 Public IPs in "inventory" manually
└───────┬────────┘
│
run work.sh
│
┌───────▼────────┐
│  Website LIVE   │ ✔ Apache deployed with Git repo
└────────────────┘

````

---

## ⚙️ Files Auto-Generated by `creation.sh`

| File         | Purpose                                                                 |
|--------------|-------------------------------------------------------------------------|
| `main.tf`    | Creates 2 AWS EC2 instances (t3.micro), latest Ubuntu 22.04, tags      |
| `inventory`  | Placeholder IPs, configured for `ubuntu` user & SSH private key         |
| `ansible.cfg`| Points Ansible to the generated inventory & default SSH key             |
| `apache.yml` | Installs Apache2 & Git, clones repo, moves `index.html`, restarts Apache|

---

## 🏗️ Deployment Steps

### 🎯 STEP 1 — Run the generation script
```bash
./creation.sh
````

This generates:

* `main.tf`
* `inventory`
* `ansible.cfg`
* `apache.yml`

### 🎯 STEP 2 — Run Terraform manually

Inside `work.sh`, Terraform commands are commented:

```bash
# terraform init
# terraform apply
```

Run them manually:

```bash
terraform init
terraform apply
```

**📌 IMPORTANT:** After EC2 instances are created, press `CTRL + C` to stop Terraform — you only need the instances created.

### 🎯 STEP 3 — Add the EC2 Public IPs

Go to **AWS Console → EC2 → Copy your instance public IPs** and put them inside `inventory`:

```ini
[ansible]
3.122.111.22
54.165.85.102

[ansible:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=/home/eissa/Downloads/key.pem
```

### 🎯 STEP 4 — Disable Terraform in `work.sh`

Keep Terraform commands commented:

```bash
#!/bin/bash

# terraform init
# terraform plan
# terraform apply

ansible-playbook apache.yml
```

### 🎯 STEP 5 — Run the Deployment

```bash
./work.sh
```

🔥 Result: Ansible automatically configures both instances and deploys your website from GitHub.

🎉 Congratulations — full automation achieved! Zero manual Apache setup. Zero Git clone. Zero copy-paste.

---

## 🌐 Features

| Feature                                 | Status |
| --------------------------------------- | ------ |
| Automatic server creation via Terraform | ✔      |
| Auto-generated Ansible inventory        | ✔      |
| Apache installation & startup           | ✔      |
| Git repo deployment                     | ✔      |
| Multi-instance support                  | ✔      |
| Fully script-driven pipeline            | ✔      |
| Zero downtime upgrades                  | ✔      |
| Production-ready structure              | ✔      |

---

## 🛠️ Technologies

| Layer      | Tools            |
| ---------- | ---------------- |
| IaC        | Terraform        |
| Automation | Ansible          |
| Web Server | Apache2          |
| Repo       | GitHub           |
| OS         | Ubuntu 22.04 LTS |
| Cloud      | AWS EC2          |

---

## 📈 Future Roadmap

* Add CI/CD using GitHub Actions
* Auto SSL via Let’s Encrypt
* Use Application Load Balancer (ALB)
* Add Auto-Scaling Group
* Convert deployment into Docker containers
* Add Netdata monitoring role

---

## 👤 Author

**Abdullrahman Eissa** – Linux System Administrator | DevOps Enthusiast
[GitHub](https://github.com/AbdullrahmanEissa) | [LinkedIn](https://www.linkedin.com/in/abdullrahman-eissa/)

```
